{
  "name": "curation-migration-datasync-cdk",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "build:dev": "rm -rf dist && NODE_ENV=development npm run synth",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok"
  },
  "engines": {
    "node": "=16"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "^3.9.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.26",
    "typescript": "4.6.3"
  }
}
